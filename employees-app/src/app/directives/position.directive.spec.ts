import { PositionDirective } from "./position.directice";


describe('PositionDirective', () => {
  it('should create an instance', () => {
    const directive = PositionDirective;
    expect(directive).toBeTruthy();
  });
});
